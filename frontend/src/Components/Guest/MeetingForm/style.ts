import { Theme } from '@material-ui/core'

export default function Style (theme: Theme) {
  return {
    meetingForm: {
      // maxWidth: '40rem'
    },
    textInput: {
      maxWidth: '300rem',
      width: '100%'
    },
    h3: {
      fontSize: '3rem !important'
    },
    h4: {
      fontSize: '2.125rem !important'
    }
  }
}
